## Required: nodemon for server
`npm install -g nodemon`

## Install Dependency
`npm install`

## Run Project
`npm start`

## The server and client will be running on port 8000
visit http://localhost:8000